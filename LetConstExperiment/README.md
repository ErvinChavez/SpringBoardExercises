# Let & Const Experiment

This project demonstrates how `let`, `const`, and `var` behave in JavaScript, focusing on **variable assignment, reassignment, and hoisting**.

---

## Tasks & Observations

### Task 1: Declare a Destination Variable
- Used `let` to declare a variable:
```js
let destination = "Ancient Egypt";
console.log(destination); // "Ancient Egypt"