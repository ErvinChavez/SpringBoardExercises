# 📦 Express Shopping List API

## Overview
A simple CRUD API built with **Express** that allows you to manage a shopping list.  
Data is stored in memory (`fakeDb.js`) and will reset when the server restarts.

---

## 🚀 Features
- **GET /items** — List all items  
- **POST /items** — Add a new item  
- **GET /items/:name** — Get a single item by name  
- **PATCH /items/:name** — Update an item’s name and/or price  
- **DELETE /items/:name** — Delete an item  

---

## 💻 Installation

1. Clone the repository:

```bash
git clone <your-repo-url>
cd ExpressShoppingListProject